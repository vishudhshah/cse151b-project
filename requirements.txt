# Packages NOT pre-installed in the course image — the only ones that need downloading.
# Everything else (torch 2.11+cu128, transformers, accelerate, datasets, sympy, numpy,
# tqdm, torchaudio, torchvision) is already in the image and inherited via --system-site-packages.

bitsandbytes>=0.46.1          # >=0.46.1 required on Python 3.13
antlr4-python3-runtime==4.11.1  # exact version required by sympy.parsing.latex
peft>=0.10.0
trl>=0.8.6
