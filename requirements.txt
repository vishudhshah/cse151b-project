# ── PyTorch — pinned to versions tested on DataHub A30 (CUDA 12.2 driver) ────
# Install torch first before this file, then use this file with -c constraints.txt
# See setup instructions in PROJECT_GUIDE.md Section 10 or README.md
#
# Quick setup (copy-paste):
#   uv venv .venv --seed && source .venv/bin/activate
#   uv pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   uv pip install -r requirements.txt -c constraints.txt
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# ── HuggingFace stack ──────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.27.0
bitsandbytes>=0.43.0          # 4-bit (NF4) quantisation

# ── Scoring / math ─────────────────────────────────────────────────────────────
sympy>=1.12
antlr4-python3-runtime==4.11.1  # exact version required by sympy.parsing.latex
numpy>=1.24

# ── Utilities ──────────────────────────────────────────────────────────────────
tqdm>=4.66

# ── Fine-tuning — Model 3 only ─────────────────────────────────────────────────
peft>=0.10.0
trl>=0.8.6
datasets>=2.18.0
