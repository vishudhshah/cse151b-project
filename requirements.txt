# ── PyTorch — pinned to version tested on DataHub (CUDA 13.0 driver) ─────────
# torchvision/torchaudio are excluded — not needed and have no cp313 wheels.
# Do NOT run `uv pip install -r requirements.txt` directly; use the two-step
# install in DATAHUB_GUIDE.md Section 5 (torch from PyTorch index first).
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

torch==2.5.1

# ── HuggingFace stack ──────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.27.0
bitsandbytes>=0.46.1          # >=0.46.1 required on Python 3.13

# ── Scoring / math ─────────────────────────────────────────────────────────────
sympy>=1.12
antlr4-python3-runtime==4.11.1  # exact version required by sympy.parsing.latex
numpy>=1.24

# ── Utilities ──────────────────────────────────────────────────────────────────
tqdm>=4.66

# ── Fine-tuning — Model 3 only ─────────────────────────────────────────────────
peft>=0.10.0
trl>=0.8.6
datasets>=2.18.0
